#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>

typedef int* board_t;

enum side {WHITE, BLACK}; 

void initialize(board_t board)
{

	return;
}

int main(int argc, char **argv)
{
	int *board = calloc(120, sizeof(int));
	int fiftyMoves = 0;
	int threeRep = 0;
	bool enPassant = false;
	return 0;
}
